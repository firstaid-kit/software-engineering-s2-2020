const asset = require('assert');
const { assert } = require('console');

describe('a test to check length of hello', () => {

    it('hello test', () => {
        assert("hello".length == 5);
    });
});